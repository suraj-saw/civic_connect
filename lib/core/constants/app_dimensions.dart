class AppDimensions {
  AppDimensions._();

  // Shared corner radius for prominent card-like surfaces.
  static const double cardRadius = 18;

  // Standard list card heights used across major pages.
  static const double issueCardMinHeight = 108;
  static const double notificationCardMinHeight = 88;
  static const double actionCardMinHeight = 104;
  static const double stepCardMinHeight = 84;
  static const double compactCardMinHeight = 72;

  // Fixed slot for 2-line supporting text in cards.
  static const double twoLineTextHeight = 34;
}
